import type { JSX } from "react";

/** Scaffold: dead transitions, deadlocks, and loops arrive with the reachability pass (M4). */
export function StructureTab(): JSX.Element {
  return (
    <p className="text-slate-400 text-sm">
      Structure &amp; diagnostics — dead transitions, deadlocks, and loops — arrive with the
      reachability pass.
    </p>
  );
}
